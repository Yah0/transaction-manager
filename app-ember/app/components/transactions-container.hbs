{{#each this.sortedTransactions as |transaction|}}
    {{#if transaction.id}}
        <TransactionTile @transaction={{transaction}} @sortedTransactions={{this.sortedTransactions}} />
    {{/if}}
{{/each}}